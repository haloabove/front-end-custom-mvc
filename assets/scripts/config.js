if (typeof app == 'undefined') {
    var app = {};
}
app.config = {
    server_configuration: {
        url: 'http://'
    },
    filters: {
        name: true,
        category: true,
        date: true
    }

}